---
title: opencodex
description: 面向 OpenAI Codex 的通用 provider 代理 —— 在 Codex CLI、App 和 SDK 中使用任意 LLM。
template: splash
hero:
  tagline: 在 OpenAI Codex 中使用任意 LLM —— Anthropic、Google、xAI、Kimi、Ollama Cloud,以及任意 OpenAI 兼容端点。一个代理,无需修补 Codex。
  actions:
    - text: 快速开始
      link: /opencodex/zh-cn/getting-started/installation/
      icon: right-arrow
      variant: primary
    - text: 工作原理
      link: /opencodex/zh-cn/getting-started/how-it-works/
      icon: open-book
    - text: GitHub
      link: https://github.com/lidge-jun/opencodex
      icon: external
      variant: minimal
---

import { Card, CardGrid, LinkCard } from '@astrojs/starlight/components';

**opencodex**(`ocx`)是一个本地代理,位于 [OpenAI Codex](https://openai.com/codex)
与任意 LLM provider 之间。Codex 只会说 **Responses API**(`/v1/responses`);opencodex 会将
该协议双向翻译 —— 包括 streaming、工具调用、推理和图像 —— 转换为你的 provider 所需的
任意线路格式,然后再把响应翻译回来。

对 Codex 原生 ChatGPT 认证路径,opencodex 也可以管理**多账号池**。已有 thread 会继续使用
最初选中的账号,适合 SSH、tmux 和移动端接续的长会话;新会话可在刷新额度后自动选择用量更低且健康的账号。

当前 GPT-5.6 Sol/Terra/Luna rollout 是 **preview-gated**。opencodex 会在具备权限的
ChatGPT/Codex 目录、OpenAI API key(`openai-apikey`: `gpt-5.6-sol`、`gpt-5.6-terra`、
`gpt-5.6-luna`)以及 OpenRouter(`openai/gpt-5.6-*`)路径中准备这些模型名；三者都使用
372,000 usable token 上下文,并保留独立的 `max` reasoning tier。实际调用是否成功取决于你的账号或
provider 是否拥有 preview 权限。

```
Codex CLI / App / SDK ──/v1/responses──▶ opencodex ──▶ Any provider
                                              │
                  Anthropic · Google · xAI · Kimi · Ollama Cloud · Groq
                  OpenRouter · Azure · DeepSeek · GLM · …and OpenAI itself
```

<CardGrid stagger>
  <Card title="任意 provider" icon="puzzle">
    五个 adapter 覆盖 Anthropic Messages、Google Gemini、Azure、OpenAI Responses 直通,
    以及**所有 OpenAI 兼容的 Chat Completions** 端点 —— 即开箱即用的 **40+ provider**。
  </Card>
  <Card title="OAuth 或 API key" icon="approve-check">
    使用你的 xAI、Anthropic 或 Kimi 账号登录(OAuth,自动刷新)、转发你的 ChatGPT
    登录凭据,或粘贴一个 API key。Key 可以是 `${ENV_VARS}`。
  </Card>
  <Card title="ChatGPT 账号池" icon="puzzle">
    添加多个 Codex/ChatGPT 账号,在仪表盘刷新 5 小时 / 每周 / 30 天额度,并让新会话自动路由到
    用量最低的可用账号,同时不移动已有 thread。
  </Card>
  <Card title="无缝接入 Codex" icon="rocket">
    `ocx init` 会向 `$CODEX_HOME/config.toml`（默认 `~/.codex/config.toml`）注入一个 provider,
    并写入共享的 Codex 目录，让 Codex CLI、TUI、App 和 SDK 看到同一组路由模型与 subagent 选择。
    `ocx stop` 可干净地恢复原生 Codex。
  </Card>
  <Card title="Subagent 与传输方式" icon="puzzle">
    最多可将 5 个路由或原生模型优先展示给 Codex `spawn_agent`。
    默认传输是 HTTP/SSE；只有设置 `websockets: true` 时才会广告 Responses WebSocket。
  </Card>
  <Card title="GPT-5.6 preview 就绪" icon="rocket">
    Sol、Terra、Luna 会以 preview-gated 方式进入原生 ChatGPT 路径以及 OpenAI API key/OpenRouter
    fallback 列表。目录中会显示 372k usable context 与 `max` reasoning。
  </Card>
  <Card title="Sidecar" icon="magnifier">
    通过你的 ChatGPT 登录凭据,借助 `gpt-5.4-mini` sidecar 路由相关能力,
    让非 OpenAI 模型也拥有真正的**网络搜索**和**图像理解**。
  </Card>
</CardGrid>

## 快速开始

```bash
# Install
npm install -g @bitkyc08/opencodex

# Interactive setup (writes config + injects into Codex)
ocx init

# Start the proxy
ocx start

# Use Codex normally — it now routes through opencodex
codex "Write a hello world in Rust"
```

## 下一步去哪里

<CardGrid>
  <LinkCard title="安装" href="/opencodex/zh-cn/getting-started/installation/" description="安装 ocx、前置条件以及验证。" />
  <LinkCard title="工作原理" href="/opencodex/zh-cn/getting-started/how-it-works/" description="完整的请求生命周期:解析 → 路由 → adapter → 桥接 → SSE。" />
  <LinkCard title="Provider" href="/opencodex/zh-cn/guides/providers/" description="OAuth、API key 目录、转发/直通,以及完整的 provider 列表。" />
  <LinkCard title="Codex App 模型选择器" href="/opencodex/zh-cn/guides/codex-app-models/" description="路由后的 opencodex 模型如何通过共享目录出现在 Codex App 中。" />
  <LinkCard title="Web 仪表盘" href="/opencodex/zh-cn/guides/web-dashboard/" description="管理 providers、OAuth、模型可见性、subagent 与日志。" />
  <LinkCard title="CLI 参考" href="/opencodex/zh-cn/reference/cli/" description="每一个 ocx 命令与参数。" />
</CardGrid>
