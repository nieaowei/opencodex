# 040 — PR #158 absorb: web-search client aborts as 499, not upstream 502

## 1. Scope and locked inputs

Absorb community PR #158 from immutable source head
`67b660bc2ae56bc39eb212620b5ee72c43946e22` (`codex/source-pr158-67b660bc`)
onto local `dev` with maintainer repairs. No push. Attribution per `000_plan.md`.

Source delta (4 files, +158/−18):

- `src/lib/errors.ts` — NEW `isClientClosedMessage`; `classifyError` preserves
  explicit `client_cancelled`, maps 499/client-close text →
  `invalid_request_error`/`client_closed_request`;
  `inferHttpStatusFromAdapterMessage` returns 499 for client closes;
  `adapterFailureFromMessage` adds 499 → `client_closed_request` type;
  `httpStatusFromTerminalError` checks codes + message before 400 fallback.
- `src/server/request-log.ts` — `requestLogErrorCode` defense-in-depth 499
  classification; `addFinalRequestLog` remaps 5xx+client-close message → 499 and
  forces `closeReason: client_cancel`.
- Tests: `errors-adapter-failure.test.ts`, `request-log.test.ts` (deferred SSE
  499 mapping, legacy 502 remap, terminal-error 499 mapping).

`git merge-tree --write-tree dev codex/source-pr158-67b660bc` → clean
(`e7a896831cbe`).

## 2. Sol review verdict (2026-07-19, lane B)

`VERDICT PR#158: GO-WITH-FIXES (blockers=1)`

| # | Sev | Finding | Disposition |
|---|-----|---------|-------------|
| 1 | P1 | Broad substring matching ("client closed", "client cancelled", …) runs globally in classifyError/inferHttpStatus; a real upstream 502 like "upstream HTTP client closed idle connection" would be misclassified 499. Local producer emits exactly "client closed request during web-search" (src/web-search/loop.ts:318,339,368) | FOLD — maintainer repair: narrow the matcher to the locally produced contract: exact phrase `client closed request` plus the explicit `request cancelled/canceled by client` forms; drop bare `client closed`/`client cancelled` substrings. Add a negative test: "upstream HTTP client closed idle connection" must remain 502 |
| 2 | P2 | Compact-path preservation untested (formatErrorResponse at responses.ts:1606/1618/1726 has no compact-abort test); combo covered by existing e2e | FOLD — DECIDED (activating, round-3 amendment): `formatErrorResponse` is exported from `src/bridge.ts:917`, not server/responses, and calling it directly would not activate the compact branch. The test instead drives the real compact path: `tests/request-log.test.ts` (which already builds server-level fixtures) gains a case invoking the compact handler (`handleResponsesCompact`) with a request whose AbortSignal fires mid-stream, asserting the terminal payload keeps `type: client_cancelled` / HTTP 499. If driving the full handler proves impractical, a narrowly scoped injection seam on the compact cancellation branch is added in the SAME repair commit — a non-activating formatter unit test does not satisfy this fold |

Reviewer confirmed: explicit `client_cancelled` ordering correct, legacy remap
driven by test, `closeReason: client_cancel` has no behavioral consumer beyond
persistence — no phantom refs. `git apply --check` clean on dev.

## 3. Landing plan

- Commits 1–2 (Wibias author): sequential picks of `57d87945` then `67b660bc`
  (two separate logical steps; DECIDED: no squash).
- Commit 3 (maintainer + Co-authored-by): matcher narrowing + negative test +
  compact-path coverage.

## 4. Verification

- `bun run typecheck` exit 0.
- `bun test tests/errors-adapter-failure.test.ts tests/request-log.test.ts` pass.
- C-ACTIVATION: negative test proves a non-web-search "client closed idle
  connection" 502 stays 502; positive tests prove 499 remap fires.
